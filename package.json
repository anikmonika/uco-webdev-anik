{
    "private": true,
    "name": "my-package",
    "version": "1.0.0",
    "scripts": {
        "dev": "vite",
        "build": "babel src -d lib",
        "test": "jest"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "my-package": "file:"
    }
}
